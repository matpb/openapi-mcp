# OpenAPI Spec Configuration (required)
OPENAPI_SPEC_URL=https://example.com/api/openapi.json
OPENAPI_API_KEY=  # Optional - only if spec requires authentication
SPEC_CACHE_TTL=300  # Cache TTL in seconds

# MCP Server Configuration
MCP_TRANSPORT=stdio
MCP_PORT=8080

# Authentication (comma-separated API keys for HTTP transport)
API_KEYS=

# CORS Configuration
CORS_ORIGIN=*
